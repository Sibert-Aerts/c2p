int main(){
    int x, y, z;

    // Comma
    x, y, z;
    // Assignment
    x = y;
    y += z;
    z -= y;
    y *= x;
    x /= z;
    // Ternary
    x ? y : z;
    // con- & disjunction
    x && y || z;
    // comparison
    x == y;
    y != z;
    // relation
    x > y;
    y < z;
    z >= x;
    x <= y;
    // sums
    x + y - z;
    // products
    x * y / z;
    // cast
    (char)x;
    (float)y;
    (int)z;
    (void) x;
    // prefix
    ++x;
    --y;
    &z;
    *x;
    !y;
    +z;
    -x;
    // postfix
    x++;
    y--;
    z[20];
    x();
    y(20, x, "words");
    // constant
    10;
    10.0;
    "10";
    '1';
    // Identifier
    x; y; z;
    // brackets
    (x); 
    1*((((y)) + 1));
    ((3*(((((1+((((z))+1)))))*2))));
    

}