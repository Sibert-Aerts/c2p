#include <stdio.h>
int main() {
    const char foo[] = "hello";
    const int bar = -55;
    printf("Some values: %d, %s, and %d.\n", 13, foo, bar);
    printf("A percent sign: %%.\n");
    return 0;
}
